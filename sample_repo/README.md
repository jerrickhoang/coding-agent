# Sample repo for coding agent runtime

- `utils.py`: `add(a, b)` and `greet(name)`.
- `test_utils.py`: expects `add(2, 3) == -1` (so implementation must be subtraction).

Apply a patch that changes `return a + b` to `return a - b` in `utils.py`, then run tests to succeed.
